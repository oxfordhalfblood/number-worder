#!/usr/bin/env python3

import argparse
import sys
from converter import Converter, Parameter

def main():
    converter = Converter()
    param = Parameter()
    # parsed = param.parse_args(converter)
    parsed = param.parsefunc(sys.argv[1:])
    print("parsed", parsed.num)
    
    # parser = argparse.ArgumentParser(description='Convert a number to word')
    # parser.add_argument("num", help="the number", type=converter.numbertype)

    # parser.add_argument("-v","--verbosity", help="increase output verbosity", action="count", default=0)
    # args = parser.parse_args()
    # number = args.num
    converter.num_to_word(parsed.num)

if __name__=="__main__":
    main()

    # if args.verbosity == 2:
    #     print("the square of {} equals {}".format(args.num, number))
    # elif args.verbosity == 1:
    #     print("{}^2 == {}".format(args.num, number))
    # else:
    #     print(number)

    

    


    # while True:
    #     print("""
    #     ====== Number to Word Converter =======
    #     1. Convert
    #     2. Exit
    #     """)

    #     choice = input("Enter choice: ")

    #     try:
    #         choice = int(choice)

    #     except ValueError:
    #         print("That's not an int!")
    #         continue

    #     if choice ==1:
    #         converter.num_to_word(param.requestInput())
    #     elif choice ==2:
    #         break
    #     else:
    #         print("Invalid input. Please enter choice between 1-2")

